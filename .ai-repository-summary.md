# AI Repository Summary

> This file provides a concise overview for AI agents and developers working with this codebase.

## Project Overview

**OS-APOW** (Open Source Agentic Process Orchestration Workflow) is a headless agentic orchestration platform that uses GitHub Issues as a work queue and executes tasks via DevContainers.

### Core Technology Stack

- **Language:** Python 3.12+
- **Framework:** FastAPI
- **Package Manager:** uv
- **HTTP Client:** httpx
- **Validation:** Pydantic v2
- **Settings:** pydantic-settings
- **Testing:** pytest + pytest-asyncio
- **Containerization:** Docker

### Four Pillars Architecture

| Pillar | Component | File Location |
|--------|-----------|---------------|
| The Ear | Notifier (API) | `src/os_apow/notifier/service.py`, `src/os_apow/api/` |
| The State | Queue | `src/os_apow/queue/github_queue.py`, `src/os_apow/services/queue.py` |
| The Brain | Sentinel | `src/os_apow/sentinel/orchestrator.py`, `src/os_apow/services/sentinel.py` |
| The Hands | Worker | `src/os_apow/worker/`, `src/os_apow/services/worker.py` |

## Project Structure

```
├── src/os_apow/
│   ├── __init__.py              # Package root with re-exports
│   ├── main.py                  # CLI entry point (sentinel/notifier)
│   ├── config.py                # Dataclass-based configuration
│   ├── api/                     # FastAPI routers
│   │   ├── __init__.py
│   │   ├── health.py            # Health check endpoints
│   │   └── webhook.py           # Webhook ingestion router
│   ├── config/                  # Pydantic settings
│   │   ├── __init__.py
│   │   └── settings.py          # Pydantic BaseSettings
│   ├── models/
│   │   ├── __init__.py
│   │   └── work_item.py         # WorkItem, TaskType, WorkItemStatus, scrub_secrets
│   ├── notifier/
│   │   ├── __init__.py
│   │   └── service.py           # FastAPI webhook receiver app
│   ├── queue/
│   │   ├── __init__.py
│   │   └── github_queue.py      # ITaskQueue ABC + GitHubQueue implementation
│   ├── sentinel/
│   │   ├── __init__.py
│   │   └── orchestrator.py      # Sentinel polling loop + task lifecycle
│   ├── services/                # Service facades
│   │   ├── __init__.py
│   │   ├── sentinel.py          # Sentinel service facade
│   │   ├── queue.py             # Queue service facade
│   │   └── worker.py            # Worker service facade
│   └── worker/
│       └── __init__.py          # Worker placeholder
├── tests/
│   ├── conftest.py              # Shared fixtures
│   ├── test_placeholder.py      # Basic import tests
│   ├── test_api/
│   │   ├── test_webhook.py
│   │   └── test_health.py
│   ├── test_models/
│   │   └── test_work_item.py
│   └── test_services/
│       ├── test_sentinel.py
│       ├── test_queue.py
│       └── test_worker.py
├── docker/
│   ├── Dockerfile               # Multi-stage production build
│   └── docker-compose.yml       # Profile-based compose
├── docs/
│   ├── adr/                     # Architecture Decision Records
│   ├── architecture/            # Architecture docs
│   └── api/                     # API docs
├── scripts/
│   ├── setup-dev.sh             # Dev environment setup
│   └── ...
├── Dockerfile                   # Root-level Dockerfile
├── docker-compose.yml           # Root-level compose
├── pyproject.toml               # Project config + deps
├── .env.example                 # Environment template
├── .python-version              # Python 3.12
└── README.md
```

## Key Files

### Entry Points

- `src/os_apow/main.py` - Main CLI entry point (argparse-based)
- `src/os_apow/notifier/service.py` - FastAPI app for webhooks
- `src/os_apow/sentinel/orchestrator.py` - Sentinel orchestrator class

### Core Models

- `src/os_apow/models/work_item.py` - WorkItem, TaskType, WorkItemStatus, scrub_secrets()
- `src/os_apow/config.py` - Configuration dataclasses (GitHubConfig, SentinelConfig, etc.)
- `src/os_apow/config/settings.py` - Pydantic BaseSettings alternative

### Queue Interface

- `src/os_apow/queue/github_queue.py` - ITaskQueue ABC and GitHubQueue implementation

## Work Item Status Flow

```
agent:queued → agent:in-progress → agent:success / agent:error / agent:infra-failure
```

## Environment Variables

| Variable | Component | Purpose |
|----------|-----------|---------|
| `GITHUB_TOKEN` | All | GitHub API authentication |
| `GITHUB_ORG` | Sentinel | Target organization |
| `GITHUB_REPO` | Sentinel | Target repository |
| `WEBHOOK_SECRET` | Notifier | Webhook signature validation |
| `SENTINEL_BOT_LOGIN` | Sentinel | Distributed locking via assignment |
| `DEBUG` | All | Enable debug logging |

## Development Commands

```bash
# Install dependencies
uv sync

# Run tests
uv run pytest

# Run notifier
uv run os-apow notifier

# Run sentinel
uv run os-apow sentinel

# Lint
uv run ruff check src/ tests/

# Format
uv run ruff format src/ tests/

# Type check
uv run mypy src/
```

## Important Patterns

### Distributed Locking (Assign-Then-Verify)

The sentinel uses an assign-then-verify pattern for task claiming:
1. Assign bot account to issue
2. Re-fetch issue to verify assignment
3. Only then update labels and proceed

### Secret Scrubbing

All output posted to GitHub is scrubbed using regex patterns in `work_item.py` to prevent credential leakage.

### Heartbeat Pattern

Long-running tasks post periodic heartbeat comments to keep observers informed.

## Testing Strategy

- Unit tests in `tests/`
- Fixtures in `tests/conftest.py`
- Async tests use `pytest-asyncio`
- Mock httpx client for API testing

## CI/CD

Existing workflows in `.github/workflows/`:
- `validate.yml` - Lint, scan, test
- `orchestrator-agent.yml` - AI orchestration
- `publish-docker.yml` - Docker image publishing
- `prebuild-devcontainer.yml` - Devcontainer prebuilding
